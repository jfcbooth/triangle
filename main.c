#include <stdio.h>
#include <math.h>

void draw(int n){
	// Find needed space
	int digits_of_n = floor(log10(n)) + 1;
	printf("%d\n", digits_of_n);
	for(int current_row = 1; current_row < n; current_row++){
		
	}
}
int main(void){
	draw(5);	
}
